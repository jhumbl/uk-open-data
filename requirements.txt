# Exact pins are load-bearing: parquet bytes embed the pyarrow version and
# pandas behaviour changes between majors, so a version bump flags every
# parquet as "changed" once. Bump deliberately and say so in the commit
# message (see the determinism gotcha in CLAUDE.md).
requests==2.31.0
pandas==3.0.3
odfpy==1.4.1
pyarrow==24.0.0
huggingface_hub==1.23.0
